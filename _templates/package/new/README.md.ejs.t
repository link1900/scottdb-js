---
to: packages/<%= name %>/README.md
---
# `@link1900/<%= name %>`

> <%= h.changeCase.sentenceCase(name) %>

## Install

Using npm:

```sh
npm install @link1900/<%= name %>
```

or using yarn:

```sh
yarn add @link1900/<%= name %>
```
